/** TMS plain inputs (modals, forms, selects). */
export const NEXUS_FORM_CONTROL =
  'nexus-field-input nexus-field-input--plain w-full min-h-[40px] py-2 text-sm';

/** Search fields with leading icon (use inside `relative` + Search icon). */
export const NEXUS_SEARCH_CONTROL = 'nexus-field-input w-full text-sm';

/** Multi-line fields. */
export const NEXUS_TEXTAREA_CONTROL =
  'nexus-field-input nexus-field-input--plain w-full min-h-[80px] py-2 text-sm resize-y';
